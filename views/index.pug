doctype html
html(lang="en").h-100
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body.h-100
    div#container.container-fluid.h-100
    script(src='/assets/application.js')
